setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
    worker: backend/Dockerfile
release:
  image: worker
  command:
    - python manage.py collectstatic --noinput
run:
  web: gunicorn woodie_api.wsgi
  
